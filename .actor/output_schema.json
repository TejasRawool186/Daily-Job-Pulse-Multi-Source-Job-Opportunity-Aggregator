{
    "actorOutputSchemaVersion": 1,
    "title": "Daily Job Pulse Output",
    "description": "Interactive dashboard, job dataset, and run analytics",
    "type": "object",
    "properties": {
        "dashboard": {
            "type": "string",
            "title": "📊 Interactive Dashboard",
            "description": "Beautiful interactive dashboard with search, filters, sorting, dark/light mode, and CSV export",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/dashboard.html"
        },
        "runSummary": {
            "type": "string",
            "title": "📈 Run Summary (JSON)",
            "description": "Machine-readable run analytics with per-source statistics and timing",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/run-summary"
        },
        "jobs": {
            "type": "string",
            "title": "💼 Job Listings (JSON)",
            "description": "Raw job data in JSON format for integration with other tools",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "jobsCSV": {
            "type": "string",
            "title": "📥 Download CSV",
            "description": "Download all job listings as a CSV file",
            "template": "{{links.apiDefaultDatasetUrl}}/items?format=csv"
        },
        "jobsExcel": {
            "type": "string",
            "title": "📊 Download Excel",
            "description": "Download all job listings as an Excel spreadsheet",
            "template": "{{links.apiDefaultDatasetUrl}}/items?format=xlsx"
        }
    }
}